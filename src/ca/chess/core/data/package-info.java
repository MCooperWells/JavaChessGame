package ca.chess.core.data;

/*
	The following is information on the DEFAULTS and OVERRIDDEN data the is hard coded into the game.
	
	GameDATA stores creates DEFAULT information regarding chess piece position and whether they are alive or not.
	
	The data is Overridden by a LoadGame() method that overrides the DEFAUlt data if it exists or it creates new values if
	GameDATA was null and empty, this doesn't require overloading because there is nothing or overload.
	
	
	Any more information about this package can be added here...


*/